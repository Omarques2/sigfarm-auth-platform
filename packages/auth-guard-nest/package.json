{
  "name": "@sigfarm/auth-guard-nest",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@nestjs/common": "^11.1.8",
    "@sigfarm/auth-contracts": "file:../auth-contracts",
    "jose": "^6.1.3"
  }
}